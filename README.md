# Welcome to your Expo app with risevest to build a Grocery Mobile app 👋

This is the Grocerya UI redesign from figma to react native

### Assignment 2: change the expo router to react-navigation which is more manual
### and create bottom tabs

1. uninstall expo router with (npm uninstall expo-router)
3. i created an index.js at the root folder same level with package.json
3. installation of react navigation from doc
> npm install @react-navigation/native
4. installed react native navigation dependencies
> npx expo install react-native-screens react-native-safe-area-context











## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```

In the output, you'll find options to open the app in a



## Get a fresh project
## don't do this !!!

```bash
npm run reset-project
```

